# 路线图 · Roadmap

这里写的是**还没做、为什么还没做**。已经做了的在 [方法论 · 审计记录](docs/methodology.md#七审计记录)。

格式固定：要解决什么 → 打算怎么做 → 为什么现在还不动。

---

## 1. 主题比对：从「任意成员重合 2 个实词」改成更不容易传递的判定

**要解决什么。** 二元组重合是粗的，而且新条目是跟组内**任意**成员比对，A 像 B、B 像 C，A 和 C 毫不相干也会进同一个组。线上已经长出过 14 条的大组。

**打算怎么做。** 三个候选，按侵入性排：
- 只跟组的**种子**（第一条）比对，切断传递链。
- 把 `minOverlapGrams` 从 2 提到 3，并要求重合的实词里至少一个不是「人称 + 单字动词」形态。
- 用一个很小的本地 embedding（CPU 可跑的那种）做二次确认，二元组只做粗筛。

**为什么还不动。** 前两个会让真正同主题的短条目归不到一起；第三个引入依赖，违背「`src/` 零依赖」。要先在线上积累一个月的归组记录，拿真数据对比三种判定的误归 / 漏归，再定。现在只有停用词这一个旋钮。

## 2. 话题池暴露「组」而不是「组的代表」

**要解决什么。** 现在解锁的组在池里占一个位子，给的是最新一条 + `groupSize`。对话层看不到组里其他几条说了什么，只知道「这件事出现过 N 次」。

**打算怎么做。** 池条目加 `members: [{id, ts, text(截断)}]`，对话层自己决定引用哪一条。

**为什么还不动。** 改的是池的 schema，消费方（对话层的 Proust 通道）要同步改。等 1 稳定了一起动，避免两次迁移。

## 3. 把「说出口」从人工标记变成可验证的事件

**要解决什么。** `spoken` 现在由对话层在用了某条记忆之后回调标记。它是整个系统唯一的续命路径，但它本身是一个信任输入——对话层说用了就算用了。

**打算怎么做。** 对话层回调时附上实际输出文本的片段，森林侧用同一套 `grams()` 验证片段与条目有 ≥2 个实词重合，不重合不算说出口。

**为什么还不动。** 这会把森林和对话层的文本格式耦合起来。要先确定对话层的输出协议不会再大改。

## 4. 寐川的「一晚」与出差 / 时差

**要解决什么。** 睡眠窗口是固定的本地时间。换时区、熬夜、早起，窗口就不对，沉寐要么写不进要么写在白天。

**打算怎么做。** 允许从外部（比如手机的睡眠数据）推送「今晚实际入睡 / 醒来时刻」覆盖默认窗口，一晚一次。

**为什么还不动。** 数据源不稳定，宁可窗口偶尔不对，也不要让一个不可靠的外部输入决定能不能做梦。

## 5. 归档瞳荧的导出

**要解决什么。** 归档 30 天后真删。删是设计目标，但「孤悬清单」本身有价值——它是「这段时间想过但没说」的记录。

**打算怎么做。** 删前可选地把孤悬条目的**文本摘要**（不含分数、不含情绪底色）追加到一个只写不读的纯文本文件。森林不读它，话题池不读它，它不参与任何机制。

**为什么还不动。** 它和「不留副本」只差一线。要先想清楚这个文件算不算记忆——如果它永远不会被任何东西读，那它是日记不是记忆，可以做；如果有一天有人想把它接回来，就不该做。现在倾向于做，但要把「只写不读」写进不变量。

---

## 不会做的

- **自动生根。** 任何形式的「置信度够高就跳过人工」都不做。这是不变量 4，不是待办。
- **模型自评重要性。** 不变量 5。
- **多租户。** 文件存储和人工浇水都不可扩展，这是取舍不是缺陷。
- **把长期存储做进来。** 第三层的检索问题和前两层的筛选问题分开，耦合起来两边都会变差。
