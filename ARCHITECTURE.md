# 架构 · Architecture

> README 讲的是「为什么要这样」。这份文档讲的是「它到底怎么连起来的」。
> 三层记忆各自的规则、以及一条记忆从产生到消失或固化，中间经过哪些判定。

---

## 全景

```
                    ┌──────────────────┐
                    │   情绪状态机      │  8 维数值，独立衰减时钟
                    │   Pulse Garden   │  不存记忆，只提供两样东西：
                    └────────┬─────────┘  ① 写入时的情绪底色 ② 意义值的一部分
                             │
                     读取 dimensions
                             │
   ┌─────────────────────────▼──────────────────────────────────┐
   │  第一层 · 眼睑森林 Forest of Eyelids                         │
   │                                                            │
   │   寐川 dream-stream            瞳荧 pupil-glimmer           │
   │   睡眠窗口内 · 1/晚 · 72h      清醒期 · 3/日 · 12h           │
   │        │                            │                      │
   │        │                       跨天归组 ──► 满 3n 解锁       │
   │        │                            │                      │
   │        └──────────┬─────────────────┘                      │
   │                   ▼                                        │
   │              话题池 topic pool ──────────► 对话层            │
   │                   │                        （唯一出口）      │
   │                   │  被说出口 spoken ◄──────                │
   │                   │       │                                │
   │              未说出口      └─► 续命：停止泛白 / 半衰期 12h→72h │
   │                   │                                        │
   │                   ▼                                        │
   │              孤悬 orphan ──► 到期                            │
   └───────────────────┬────────────────────────────────────────┘
                       │
          到期时判定：意义值 ≥ 0.5 ？
                       │
          ┌────────────┴────────────┐
          │ 否                       │ 是
          ▼                          ▼
       直接删除              ┌──────────────────────────┐
      （不归档）             │  第二层 · 骨骼温室         │
                            │  Greenhouse              │
                            │                          │
                            │  pending                 │
                            │     │  ◄── 人工浇水       │
                            │     ▼      （唯一的门）    │
                            │  rooted ──► cellar        │
                            └──────┬───────────────────┘
                                   │ 人工确认后投递
                                   ▼
                         ┌──────────────────────┐
                         │  第三层 · 长期存储     │
                         │  事件桶 · 语义检索     │
                         │  （本项目不含）        │
                         └──────────────────────┘
```

三层的分工可以用一句话概括：**森林决定什么值得被想起，温室决定什么值得被记住，长期层负责被记住之后还能被找到。**

---

## 一条记忆的完整生命周期

以一条瞳荧（清醒时的念头闪回）为例，走一遍全流程。

### 1. 写入前：配额检查

```
今日已写入 3 条？ ──► 拒绝（409）
```

配额是硬门槛。**孤悬的条目不退还名额**——想过就是想过，不因为没说出口就当没发生。

### 2. 写入时：去重与归组

新条目先和历史做主题比对。分词是 CJK 二元组 + 拉丁词，去停用词，**两个实词重合即判定同一件事**。

```
同一天已有同主题？  ──► 合并计数 count+1，不占配额，流程结束
跨天存在同主题？    ──► 归组
```

归组的后果是集体性的：

| | 未归组 | 已归组 |
|---|---|---|
| 半衰期 | 12h | 72h |
| 衰减底线 | 0.15 | 0.55 |

组内数量每达到 3 的倍数就**解锁**一次，解锁状态下整组直接进入话题池，不过意义值门槛。

组也会死：断供超 7 天、或建组超 7 天仍未满 3 条，整组沉底。

### 3. 写入时：意义值计算

```js
score = min(0.75,
    (aboutOther ? 0.25 : 0)   // 关于对方，而不是关于自己
  + (novel      ? 0.20 : 0)   // 第一次发生
  + (hook       ? 0.10 : 0)   // 留有未闭合的钩子
  + emoScore                  // 来自情绪状态机
)
```

前三个是二值标记，由写入方提供。它们都是**事实性问题**（这条是关于对方吗？是第一次吗？），不是价值判断（这条重要吗？）——模型在事实性问题上远比在价值判断上可靠。

`emoScore` 是模型完全碰不到的一路输入：

```js
off = |dims[main] - mean7d(main)|        // 主导维度相对近七天均值的偏移
emoScore = off > 15 ? 0.2
         : off >= 10 ? 0.15
         : 0                              // 样本不足 3 次记 0
```

含义是**在情绪反常的时刻写下的东西更重要**，且这个判断由外部状态机给出。

同时记录写入时的主/次导维度（`paper`），作为这条记忆的「情绪底色」，之后用于渲染和排序。

### 4. 存活期：读取时衰减

衰减**在读取时计算，从不写盘**：

```js
floor = pressedTo ?? (boosted ? 0.55 : 0.15)
hl    = boosted ? 72h : 12h
cur   = floor + max(0, score - floor) * 0.5 ^ (age / hl)
```

`cur` 映射三档可见度：

| cur | 状态 | 表现 |
|---|---|---|
| > 0.4 | clear 清晰 | 完整文本 |
| 0.4 – 0.2 | mist 半雾 | 文本截断 |
| ≤ 0.2 | white 几乎全白 | 近乎不可读 |

不写盘这一点是刻意的：**持久化的衰减会让状态不可复现。** 读取时算，同一份数据在任何时刻都能推出确定的结果。

### 5. 存活期：话题池准入

话题池是森林向对话层暴露的**唯一出口**。

| 类型 | 准入条件 |
|---|---|
| 瞳荧 | `cur ≥ 0.45` 且在寿命窗口内 |
| 瞳荧（成组解锁） | 直接进入 |
| 寐川 | `score ≥ 0.55` |
| 寐川（挣扎态） | 直接进入，排序置顶 |

已 `spoken` / `suppressed` / `orphan` 的一律不入池。

### 5.5 消费侧：普鲁斯特通道与玛德琳

话题池只是「森林把什么摆到了对话层面前」。但摆出来不等于会被想起——**对话层怎么消费这个池子，决定了「想起」是机械的还是像人的。** 这一层的设计直接借了普鲁斯特的意象：那块泡进茶里、勾起整段童年的玛德琳蛋糕。分两条腿：

**玛德琳 · 由当前输入勾起。** 用户最新一句话，拆成 2-gram，与池中每条算 Dice 相似度，过地板阈值就注入最相似的一条——「这句话让你忽然想起……」。不是检索命令，是联想：由用户的话撩动。为什么是 2-gram Dice 而不是 embedding——运行环境没有 GPU，2-gram Dice 是零依赖、纯 CPU、可解释的相似度，用真实对话池标定过阈值（无关话题落 0~0.13，真正共鸣落 0.25~0.67，地板卡 0.22）。四道闸门（最小间隔、触发概率、相似度地板、近期去重）防止退化成复读机。

**随机浮现 · 无触发漂移。** 无输入触发，发呆时漂过一条，被最近几句余温加权但保留底噪，偶尔漂到完全无关的旧事。模拟「没来由地想起一件事」。

**回流闭环。** 这是关键的一环：**在森林里被浮起过、够格进话题池的瞳荧与寐川，会自动并入普鲁斯特池。** 否则那些「想起的一瞬」被说出口一次就沉底，再也不会主动回来。回流让它们从此也能被用户的话勾起、也能随机漂上来——森林的「想起」和普鲁斯特的「回忆」被接成了一个环，而不是两个孤立的机制。

这条暗线在界面上完全不可见，但它是「像一个有内在生活的人、而不是只在被戳时才有反应的机器」的核心。它也解释了为什么话题池准入（5）之后紧接着是「被说出口」（6）——中间正是这一层在把「摆出来的」变成「真的想起并说出来的」。

### 6. 转折点：被说出口

`spoken` 是**唯一的续命机制**，由「这条记忆真的被拿进对话说了」触发：

- 寐川：停止泛白，活到清除窗口外
- 瞳荧：半衰期 12h → 72h，底线 0.15 → 0.55
- 清除孤悬状态
- 所属组的解锁状态复位（避免同一组反复霸占话题池）

### 7. 结局 A：孤悬

到期仍未被说出口 → 落入 `orphan`。

这不是删除的前置状态，是一个**可查看、可统计的显式状态**：想过，但没说。

### 8. 结局 B：迁入温室

到期时若 `score ≥ 0.5`，在被清除**之前**整卡迁入温室：

```js
take = (e, kind, ttl) => {
  if (migrated.has(e.id) || e.score < 0.5 || now - e.ts < ttl) return;
  push({ ...e, kind: "specimen", status: "pending",
         rings: e.spoken ? 1 : 0 });
}
```

`rings`（年轮）记录来源痕迹：迁入时已被说出口的记为 1。已迁移的 ID 进 `migrated` 集合，保证不重复迁入。

### 9. 结局 C：直接消失

到期、意义值不足、未被说出口 → **删除，不归档、不留副本**。

这是这套架构最容易被质疑、也最不该妥协的一点。详见 [方法论 · 原则一](docs/methodology.md)。

---

## 寐川的特殊规则

寐川（睡眠窗口内的梦）和瞳荧共享大部分机制，但有三处不同。

### 睡眠窗口与「一晚」的归属

```
工作日  00:30 – 08:30
周末    02:00 – 08:30   （周五、周六晚）
```

关键在于**一晚归属于它开始的那一天**，而不是跨过午夜后的日期。凌晨三点写的梦属于「昨晚」，不属于今天。没有这条规则，配额判定会在午夜前后错乱。

### 泛白

第 60 小时开始，12 小时内匀速泛白，72 小时整清除：

```js
fade = spoken ? 0 : (age > 60h ? min(1, (age - 60h) / 12h) : 0)
```

已说出口的寐川 `fade` 恒为 0——**汇入川流的就不再泛白**。

### 挣扎态

48 – 72 小时之间、未被说出口的寐川进入 `struggle`：

- 绕过 `score ≥ 0.55` 的门槛
- 强行进入话题池
- 在池内排序中置顶

模拟的是「快忘了，但还是想说」这个真实的时刻。没有这条，中等意义值的寐川会安静死掉，从来没被提起过。

---

## 情绪状态机怎么接进来

这是三层之外的一条**横向连接**，也是最容易被误解的部分。

情绪状态机**不存储任何记忆**。它只在两个时刻被读取：

1. **写入时** —— 取当前八维数值，算出主/次导维度，作为这条记忆的情绪底色
2. **写入时** —— 取主导维度相对近七天均值的偏移，折算成 `emoScore` 计入意义值

```js
async function pulseDims() {
  try { const r = await fetch(PULSE_URL); return (await r.json()).dimensions; }
  catch { return null; }        // 拿不到就降级，不阻塞写入
}
```

`pulseLog` 在森林侧维护一个七天滚动窗口，用于算均值。样本不足三次时 `emoScore` 记 0——**冷启动阶段不让噪音进入意义值**。

三点值得注意：

- **单向。** 森林读情绪，情绪不读森林。这让两个系统可以独立部署、独立失败。
- **可降级。** 状态机不可用时返回 `null`，情绪加权自动关闭，其余机制照常。
- **可替换。** 任何返回 `{dimensions: {name: number}}` 的服务都能接上。

---

## 温室：唯一的人工闸门

温室的状态机很简单：

```
pending ──root/edit──► rooted ──► cellar
   │
   └──drop──► 丢弃
```

| 动作 | 含义 |
|---|---|
| `root` 浇水 | 确认为长期记忆 |
| `edit` | 改写正文后生根（正文 ≥10 字才生效） |
| `cellar` 入窖 | 确认过，但暂不进长期记忆 |
| `stars` | 1–5 评级 |
| `drop` | 丢弃 |

**`root` 和 `edit` 是通往长期记忆的唯一两扇门，且只能由人打开。**

温室有两个入口：

1. **自动** —— 森林到期条目的迁入（蒸馏）
2. **手动** —— `POST /add`，任何蒸馏管道都可投递

「蒸馏自动，生根过手」这句话说的就是这两个入口和那一扇门的关系。

---

## 存储

文件系统上的 JSON，原子写：

```js
const t = FILE + ".tmp";
fs.writeFileSync(t, JSON.stringify(d));
fs.renameSync(t, FILE);          // 同一文件系统上 rename 是原子的
```

没有数据库。这个规模下（单用户、活跃条目上限 20 条瞳荧 + 若干寐川），JSON 的可读、可备份、可手工修复远超引入数据库的收益。

代价同样明确：**并发写会互相覆盖**（后写的赢），没有事务，每次修改都要全量读写。单用户场景下前两条几乎不触发，第三条因为数据量小也不构成问题。

多用户场景下这三条会立刻变成故障。见 README 的「适用范围」。

---

## 与长期存储的边界

息之洲**不实现**长期存储。温室 `rooted` 的条目投递到哪里，是集成方的事。

作者的部署基于 [Ombre Brain](https://github.com/P0luz/Ombre-Brain)（MIT，作者 P0lar1zzZ）。息之洲不含它的任何代码，也不依赖它的接口——换成任何事件桶存储、向量库、甚至一个纯文本文件都可以。

这条边界是有意划的：**前两层解决的是筛选问题，第三层解决的是检索问题。** 把它们耦合在一起，两边都会变差。

---

## 相关

- 每条规则背后的理由与失败过的做法 → [docs/methodology.md](docs/methodology.md)
- 完整接口 → [docs/api.md](docs/api.md)
